class ToDo{
  int id;
  String title;
  String description;
  bool is_done;
  String date;
  ToDo({
    required this.id,
    required this.title,
    required this.description,
    required this.is_done,
    required this.date,
  });

}